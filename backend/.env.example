# ============================================================================
# TaskEasy Backend - Environment Configuration
# ============================================================================
#
# Copy this file to .env and fill in your actual values.
# DO NOT commit .env to version control - it contains secrets.
#
# For Vercel:
# - Set these as Secrets in the Vercel Dashboard
# - Use vercel env pull to sync locally for vercel dev
#

# ============================================================================
# DATABASE
# ============================================================================

# MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/dbname?appName=AppName
MONGODB_URI=mongodb+srv://your_username:your_password@cluster.mongodb.net/taskeasy?appName=Cluster0

# ============================================================================
# SECURITY
# ============================================================================

# JWT signing secret (use a strong random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-min-32-chars

# ============================================================================
# DEPLOYMENT
# ============================================================================

# Backend port (only used for local development)
# Vercel automatically assigns ports, this is ignored in production
PORT=5000

# Node environment
NODE_ENV=development

# Frontend URL for CORS (Vercel deployment)
# Example: https://taskeasy-frontend.vercel.app
# Keep empty to use localhost defaults
FRONTEND_URL=

# ============================================================================
# NOTES
# ============================================================================
# 
# For local development with `vercel dev`:
#   1. Copy this file to .env
#   2. Fill in your actual values
#   3. Run: vercel env pull (to sync Vercel secrets)
#   4. Run: npm run dev (or vercel dev)
#
# For Vercel deployment:
#   1. Set MONGODB_URI as a Secret in Vercel Dashboard
#   2. Set JWT_SECRET as a Secret in Vercel Dashboard
#   3. Set FRONTEND_URL to your deployed frontend URL
#   4. Deploy: vercel --prod
#
