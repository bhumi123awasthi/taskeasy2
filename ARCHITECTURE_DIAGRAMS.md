# Time Log Summary - Architecture & Flow Diagrams

## 🏗️ Component Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    TaskEasy Application                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                ┌──────────────────────────┐
                │   Time Log Summary Page  │
                │  (timelogsummary.jsx)    │
                └──────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
    ┌────────────┐  ┌─────────────┐  ┌──────────────┐
    │   Hooks    │  │   State     │  │   Services   │
    │            │  │ Management  │  │              │
    │ useProject │  │             │  │ fetchWork    │
    │ useEffect  │  │ workItems   │  │ Items()      │
    │ useState   │  │ timeLogs    │  │              │
    └────────────┘  │ users       │  │ formatHours()│
                    │ filters     │  │              │
                    │ loading     │  │ getWeekDate  │
                    │ error       │  │ Range()      │
                    └─────────────┘  └──────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  UI Components   │
                    │                  │
                    │ • Filters        │
                    │ • Search Button  │
                    │ • Data Table     │
                    │ • Error Message  │
                    │ • Loading State  │
                    └──────────────────┘
```

---

## 📊 Data Flow Diagram

```
START
  │
  ├─► Page Loads
  │     │
  │     ├─► useEffect hooks execute
  │     │
  │     └─► loadWorkItems()
  │         │
  │         ├─► fetchWorkItems(projectId)
  │         │     │
  │         │     └─► API Call: GET /api/projects/{id}/workitems
  │         │         │
  │         │         ▼
  │         │     Backend Response
  │         │     (Array of work items)
  │         │         │
  │         │         ▼
  │         ├─► setWorkItems(data)
  │         │
  │         └─► Extract Users
  │             │
  │             ├─► Filter assignees
  │             ├─► Get unique names
  │             └─► setUsers(array)
  │
  ├─► User Selects Filters
  │     │
  │     ├─► selectedUser
  │     ├─► fromDate
  │     ├─► toDate
  │     └─► weekSelection
  │
  ├─► User Clicks "Search"
  │     │
  │     └─► handleSearch()
  │         │
  │         ├─► calculateTimeLogs()
  │         │     │
  │         │     ├─► For each workItem:
  │         │     │     ├─► Check assignee matches selectedUser
  │         │     │     ├─► Check date in range
  │         │     │     └─► Add to results
  │         │     │
  │         │     └─► Return filtered array
  │         │
  │         └─► setTimeLogs(filtered)
  │             │
  │             └─► Component Re-renders
  │
  ├─► Table Renders
  │     │
  │     ├─► getDateColumns()
  │     │     └─► Extract unique dates
  │     │
  │     ├─► getUsersFromLogs()
  │     │     └─► Extract unique users
  │     │
  │     ├─► formatHours(decimal)
  │     │     └─► Convert 5.5 → "5:30"
  │     │
  │     ├─► getTotalForDate(date)
  │     │     └─► Sum time for date
  │     │
  │     └─► getGrandTotal()
  │         └─► Sum all time
  │
  └─► DISPLAY TABLE

END
```

---

## 🔄 State Management Flow

```
┌────────────────────────────────────────────────────────┐
│             Component State Variables                   │
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │ Data State                                       │ │
│  ├──────────────────────────────────────────────────┤ │
│  │ • workItems: WorkItem[]                          │ │
│  │ • timeLogs: FilteredLog[]                        │ │
│  │ • teams: string[]                                │ │
│  │ • users: string[]                                │ │
│  └──────────────────────────────────────────────────┘ │
│                        │                                │
│                        ▼                                │
│  ┌──────────────────────────────────────────────────┐ │
│  │ Filter State                                     │ │
│  ├──────────────────────────────────────────────────┤ │
│  │ • selectedTeam: string                           │ │
│  │ • selectedUser: string                           │ │
│  │ • fromDate: string (YYYY-MM-DD)                  │ │
│  │ • toDate: string (YYYY-MM-DD)                    │ │
│  │ • weekSelection: string                          │ │
│  │ • monthSelection: string                         │ │
│  └──────────────────────────────────────────────────┘ │
│                        │                                │
│                        ▼                                │
│  ┌──────────────────────────────────────────────────┐ │
│  │ UI State                                         │ │
│  ├──────────────────────────────────────────────────┤ │
│  │ • loading: boolean                               │ │
│  │ • error: string                                  │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
└────────────────────────────────────────────────────────┘
                         │
                         ▼
                    Re-render Component
                    (React triggers)
                         │
                    ┌────┴────┐
                    ▼         ▼
              Update UI   Recalculate
              Elements    Derived Data
```

---

## 🗂️ File Structure

```
src/
├── pages/
│   └── timelogsummary.jsx ..................... Main component (541 lines)
│       └── State Management
│           ├── workItems: WorkItem[]
│           ├── timeLogs: FilteredLog[]
│           ├── users: string[]
│           ├── selectedUser: string
│           ├── fromDate, toDate: string
│           ├── loading: boolean
│           └── error: string
│
│       └── Effects & Handlers
│           ├── useEffect (mount)
│           ├── useEffect (load items)
│           ├── loadWorkItems()
│           ├── calculateTimeLogs()
│           ├── handleSearch()
│           ├── handleWeekChange()
│           ├── getDateColumns()
│           ├── getUsersFromLogs()
│           ├── formatHours()
│           ├── getTotalForDate()
│           └── getGrandTotal()
│
│       └── UI Sections
│           ├── Header
│           ├── Sidebar
│           ├── Filters
│           │   ├── Team selector
│           │   ├── User selector
│           │   ├── Week selector
│           │   ├── Month selector
│           │   ├── From date
│           │   └── To date
│           ├── Buttons
│           │   ├── Search
│           │   └── Download
│           ├── Error message
│           └── Table
│               ├── Headers
│               ├── Body rows
│               └── Footer totals
│
└── services/
    ├── workItemService.js ..................... Existing service
    │   ├── fetchWorkItems(projectId)
    │   ├── createWorkItem()
    │   ├── updateWorkItem()
    │   └── deleteWorkItem()
    │
    └── timeLogService.js ..................... New service (94 lines)
        ├── fetchTimeLogSummary()
        ├── updateTimeSpent()
        ├── formatHours()
        ├── calculateTotalHours()
        ├── groupLogsByUserAndDate()
        └── getWeekDateRange()

backend/
└── routes/
    └── workitems.js ......................... Updated with new endpoint
        └── GET /time-log-summary
            ├── Query: userId, fromDate, toDate, sprintId
            └── Response: Grouped time logs by user & date
```

---

## 🔌 API Integration

```
┌─────────────────────────────────────────────────────┐
│            Frontend Component                        │
│        (timelogsummary.jsx)                         │
└─────────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
   ┌─────────────┐ ┌──────────────┐ ┌──────────────┐
   │ Axios Call  │ │ Axios Call   │ │ Axios Call   │
   │ (Load Items)│ │ (Time Log    │ │ (Update)     │
   │             │ │  Summary)    │ │              │
   └─────────────┘ └──────────────┘ └──────────────┘
        │               │               │
        ▼               ▼               ▼
   GET /api/projects/{id}/workitems
   GET /api/projects/{id}/time-log-summary (optional)
   PATCH /api/projects/{id}/workitems/{id}
        │               │               │
        └───────────────┼───────────────┘
                        │
        ┌───────────────▼───────────────┐
        │                               │
        ▼                               ▼
    ┌──────────────────────────┐ ┌────────────────┐
    │    Express Router        │ │   MongoDB      │
    │                          │ │                │
    │ • Parse request          │ │ • Store data   │
    │ • Validate token         │ │ • Query data   │
    │ • Query database         │ │ • Return docs  │
    │ • Format response        │ │                │
    └──────────────────────────┘ └────────────────┘
                        │
        ┌───────────────▼───────────────┐
        │                               │
        ▼                               ▼
    ┌─────────────────────────────────────────┐
    │      Response (JSON)                    │
    │  {                                      │
    │    items: [ ...work items... ],         │
    │    total: 100                           │
    │  }                                      │
    └─────────────────────────────────────────┘
                        │
                        ▼
            ┌───────────────────────┐
            │  Frontend Processing  │
            │                       │
            │ • setWorkItems()      │
            │ • extractUsers()      │
            │ • calculateLogs()     │
            │ • formatDisplay()     │
            └───────────────────────┘
                        │
                        ▼
            ┌───────────────────────┐
            │   User Sees Table     │
            │   With Time Logs      │
            └───────────────────────┘
```

---

## 📈 Time Calculation Flow

```
┌─────────────────────────────────────────┐
│     Raw Work Item Data                  │
│  {                                      │
│    title: "UI Design",                  │
│    timeSpent: 5.5,           ◄─ decimal hours
│    assignees: [{name: "John"}],         │
│    timeline: {                          │
│      startDate: "2025-01-15"            │
│    }                                    │
│  }                                      │
└─────────────────────────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │   formatHours(5.5)           │
    │                              │
    │   h = Math.floor(5.5) = 5    │
    │   m = (5.5 - 5) × 60 = 30    │
    │                              │
    │   return "5:30"              │
    └──────────────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │   Display in Table           │
    │                              │
    │   User | Item | 2025-01-15 │
    │  John | Design | 5:30      │
    └──────────────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │   calculateTotals()          │
    │                              │
    │   5.5 + 3.0 + 4.5 = 13.0    │
    │                              │
    │   formatHours(13.0) = "13:00"│
    └──────────────────────────────┘
```

---

## 🔐 Security & Authentication

```
┌─────────────────────────────────────────┐
│        Browser Local Storage            │
│                                         │
│  token: "eyJhbGciOiJIUzI1NiI..."       │
│  user: { id, name, email }              │
└─────────────────────────────────────────┘
                   │
                   ▼ (Retrieved on app load)
    ┌──────────────────────────────┐
    │   Axios Request Config       │
    │                              │
    │   headers: {                 │
    │     Authorization:           │
    │       "Bearer {token}"       │
    │   }                          │
    └──────────────────────────────┘
                   │
                   ▼ (Attached to all API calls)
    ┌──────────────────────────────┐
    │   HTTP Request               │
    │                              │
    │   GET /api/projects/{id}     │
    │   Authorization: Bearer ...  │
    └──────────────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │   Backend Middleware         │
    │                              │
    │   • Extract token from       │
    │     Authorization header     │
    │   • Verify with JWT_SECRET   │
    │   • Extract userId           │
    │   • Continue if valid        │
    │   • Return 401 if invalid    │
    └──────────────────────────────┘
```

---

## 📊 Table Rendering Structure

```
Work Items Array
    │
    ├─ Item 1: {user: "John", date: "2025-01-15", time: 5.5}
    ├─ Item 2: {user: "John", date: "2025-01-15", time: 3.0}
    ├─ Item 3: {user: "John", date: "2025-01-16", time: 4.5}
    ├─ Item 4: {user: "Jane", date: "2025-01-15", time: 6.0}
    └─ Item 5: {user: "Jane", date: "2025-01-16", time: 2.0}
         │
         ▼
    getDateColumns()
         │
         ├─ "2025-01-15"
         └─ "2025-01-16"
         │
         ▼
    getUsersFromLogs()
         │
         ├─ "Jane"
         └─ "John"
         │
         ▼
    Table Rendering
    ┌────────────────────────────────────────────────┐
    │ User │ Project │ Item   │ Type  │ Jan 15 │ Jan 16 │ Total
    ├────────────────────────────────────────────────┤
    │ Jane │ Project │ Task A │ Dev   │ 6:00   │ 2:00   │ 8:00  │
    ├────────────────────────────────────────────────┤
    │      │         │        │       │        │        │       │
    │ John │ Project │ Task B │ Dev   │ 5:30   │        │ 5:30  │
    │      │         │ Task C │ Dev   │ 3:00   │        │ 3:00  │
    │      │         │ Task D │ QA    │        │ 4:30   │ 4:30  │
    ├────────────────────────────────────────────────┤
    │      │         │ TOTALS │       │ 14:30  │ 6:30   │ 21:00 │
    └────────────────────────────────────────────────┘
```

---

## ⚙️ Filter Logic

```
Original Work Items (10 items)
         │
    ┌────┴────────────────────┐
    │                         │
    ▼                         ▼
Filter 1: By User         Filter 2: By Date
selectedUser = "John"     fromDate = "2025-01-15"
                          toDate = "2025-01-20"
    │                         │
    ├─ Keep items where       ├─ Keep items where
    │  assignee.name =        │  startDate >= fromDate
    │  "John"                 │  AND
    │                         │  startDate <= toDate
    ▼                         ▼
Filtered (5 items)        Filtered (6 items)
    │                         │
    └────────┬────────────────┘
             ▼
    Apply ALL Filters
    (User AND Date Range)
             │
             ▼
    Final Results (3 items)
    (John's work in date range)
             │
             ▼
    Display in Table
```

---

## 🎯 Error Handling Flow

```
User Clicks Search
         │
         ▼
   Try Block
         │
    ┌────┴─────────────────┐
    │                      │
    ▼                      ▼
Success              Error Caught
    │                      │
    ├─ Calculate logs  ├─ console.error()
    ├─ setTimeLogs()   ├─ setError(message)
    ├─ Show table      └─ Display error UI
    └─ Clear error

Display Error Message
    │
    ▼
┌────────────────────────────────┐
│ Error: Failed to load items    │
│ [Dismiss] button               │
└────────────────────────────────┘
    │
    └─ User can:
       • Check console (F12)
       • Check network tab
       • Retry search
       • Verify backend running
```

---

## 📱 Responsive Design

```
Desktop (>1024px)
┌────────────────────────────────────────────────────────┐
│                     Header                             │
├────────┬──────────────────────────────────────────────┤
│ Sidebar│        Main Content (Time Log Table)         │
│        │                                              │
│ • Over │  Filters | Data Table                        │
│ • Board│  Search  | [dynamically sized columns]       │
│ • Work │ Download | [scrollable if needed]            │
│ • Back │          |                                    │
│ • Spri │          |                                    │
│ • Pipe │          |                                    │
└────────┴──────────────────────────────────────────────┘

Tablet (768px-1024px)
┌──────────────────────────┐
│        Header            │
├──────────────────────────┤
│ ☰ Menu                   │
├──────────────────────────┤
│                          │
│  Filters (stacked)       │
│  ┌──────────────────┐    │
│  │ Team dropdown    │    │
│  ├──────────────────┤    │
│  │ User dropdown    │    │
│  ├──────────────────┤    │
│  │ Date range       │    │
│  ├──────────────────┤    │
│  │ Search Button    │    │
│  └──────────────────┘    │
│                          │
│  Table (scrollable)      │
│  ┌──────────────────┐    │
│  │[scroll →]        │    │
│  │[scroll →]        │    │
│  └──────────────────┘    │
└──────────────────────────┘

Mobile (<768px)
┌──────────────┐
│   Header     │
├──────────────┤
│ ☰ Menu       │
├──────────────┤
│              │
│ Filters      │
│ (full width) │
│              │
├──────────────┤
│              │
│ Table        │
│ (scrollable) │
│              │
│ ← scroll → │
│              │
└──────────────┘
```

---

**Diagrams Created:** January 2025
**All diagrams are accurate as of implementation version 1.0**
